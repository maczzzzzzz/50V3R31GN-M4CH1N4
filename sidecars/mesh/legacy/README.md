# Legacy Mesh Proxy

This directory contains superseded code that is no longer in active use.

## mesh_proxy.py

Custom FastAPI router that was the original mesh proxy before LiteLLM Docker
deployment. Replaced by the `mesh-litellm` Docker container (proxy.yml).

Kept for reference only. Do not run.

Superseded: 2026-05-17 (Docker Desktop migration)
