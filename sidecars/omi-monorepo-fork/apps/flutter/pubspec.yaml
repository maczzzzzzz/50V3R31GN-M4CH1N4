name: omi_sovereign_flutter
description: Omi Flutter Client - Sovereign Voice Layer with Artery Integration
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Networking
  http: ^1.1.0
  web_socket_channel: ^2.4.0

  # State Management
  provider: ^6.1.0

  # UI
  # Note: flutter_pretext is a placeholder package name
  # In production, replace with actual package or integrate locally
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
