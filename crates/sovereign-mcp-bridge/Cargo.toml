[package]
name = "sovereign-mcp-bridge"
version = "0.1.0"
edition = "2021"
description = "VSB-to-MCP bridge: exposes hardware telemetry as MCP resources"

[dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
