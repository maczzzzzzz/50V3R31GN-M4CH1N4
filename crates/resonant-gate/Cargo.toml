[package]
name = "resonant-gate"
version = "3.8.8"
edition = "2021"
description = "Phase 77: Resonant Logic Gate — deterministic LLM output governance via decision_audit"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
tracing = "0.1"
