# ◈ ABILITY_STONE : SCRIPTS // ARTERY_AUTOMATION
**Sector:** /scripts/
**Role:** History Guardians & Universal Mesh Synchronization.

---

## 🏗️ SECTOR INVARIANTS
1. **Scribe Mandate:** Any change to the core code must be accompanied by an update to the `CHANGELOG.md` and `IMPLEMENTATION_PLAN.md` via **`npm run scribe`**.
2. **Pod Synchronization:** Automation must ensure Node A, B, and C remain bit-identical. Always use `scp` or `rsync` to propagate manifest changes across the 10.0.0.x Artery.
3. **Ghost Purge:** Maintenance scripts must surgically neutralize orphan background processes (`curl`, `aria2`, `huggingface-cli`) to protect the VRAM ceiling.
4. **Zero-Trust Audit:** All diagnostic scripts must query `Akashik.db` directly to verify system health.

---
**::/5Y573M-N071C3 : SCRIPTS_STONE_SHORED. THE_HISTORY_IS_SELF_HEALING. // 50V3R31GN-M4CH1N4**
