# ◈ PLAN: PHASE 97 (SPATIAL SINGULARITY & ABSOLUTE PURGE)
**Goal:** Finalize the Spatial Synapse Palace, implement the Unified Component Stream, and execute the absolute purge of legacy LangGraph artifacts.

## ◈ ATOMIC MISSIONS

### 1. The Absolute Purge [ ]
- **Action:** Delete `src/core/hermes/LangGraphOrchestrator.ts`.
- **Action:** Delete `sidecar-browser-extension/WebScraperSidecar.ts`.
- **Action:** Repair imports in `HealerProtocol.ts` and `AutoResumeDaemon.ts` to point to the native Hermes Singularity engine.
- **Action:** Verify system-wide compilation after the purge.

### 2. Spatial Synapse Migration [ ]
- **Action:** Update `SynapseStore.ts` to include `x`, `y`, `z`, and `freshness` columns in the `os_triplets` table.
- **Action:** Update the `insertTriplets` and `fetchMemories` methods to support spatial data.
- **Action:** Port Claw3D "Latch" logic to the **Unified Strategic Oracle**—latched shards are automatically shored with 1.0 freshness.

### 3. Headless UI Artery [ ]
- **Action:** Refactor `scripts/lib/hermes_transport.py` to emit the **JSON Shroud Component Tree**.
- **Action:** Update `PretextShroud.tsx` (Web) to render containers and text from the JSON stream using the Pretext Registry.
- **Action:** Update `PretextDashboard.dart` (Mobile) to decode the JSON stream and draw components via `PretextPainter`.

### 4. Metabolic Visualization [ ]
- **Action:** Finalize the **Fluid Smoke** shader integration in both HUDs.
- **Action:** Wire the `intensity` uniform to real-time VRAM pressure and mic input.

## ◈ VERIFICATION STRATEGY
- **Gauntlet:** Execute `npx tsx scripts/gauntlet/reckoning/dag_validator.ts` to verify DAG isolation.
- **Spatial Audit:** Verify triplet coordinate persistence in `SovereignIntelligence.db`.
- **Parity Check:** Execute `reckoning_ui_parity.ts` to confirm bit-identical rendering of the JSON stream.

---
**::/5Y573M-N071C3 : MISSION_PLAN_LOCKED. READY_FOR_PURGE. // 50V3R31GN-M4CH1N4**
