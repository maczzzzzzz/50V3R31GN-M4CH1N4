should mercury be leveraged for agentic memory boosts ? https://mercury.cosmicstack.org/

---

**DISCOVERY PHASE – COMPLETE**

**Locked Query**  
You are asking whether **Mercury** (https://mercury.cosmicstack.org/) should be leveraged for **agentic memory boosts** inside Sovereign Machina, specifically in the context of the new Profile + Plugin Framework, the clean core repo, the upcoming Cyberpunk Red plugin, and the overall NODESTADT product line.

No changes to current technical decisions (Node D setup tonight, clean model topology, database separation into `sovereignintelligence.db` + `cyberpunk.db`, etc.).

---

**RESEARCH PHASE – Mercury Analysis (Verified 2026-04-28)**

Mercury is a **self-hosted, local-first personal AI agent** built by Cosmic Stack Labs.

**Core Features Relevant to Agentic Memory**:
- **“Second Brain” memory system**: Structured, persistent long-term memory using SQLite + FTS5 full-text search.
- 10 memory types (identity, preference, goal, project, habit, decision, constraint, relationship, episode, reflection).
- Automatic fact extraction, deduplication, conflict resolution, importance/durability scoring, consolidation, and pruning.
- Recall mechanism injects relevant memories into context before each LLM call (token-efficient).
- Episodic logs + hourly synthesis/reflection passes.

**Technical Stack**:
- TypeScript + Node.js (ESM).
- Vercel AI SDK for the agent loop.
- Fully local (all data in `~/.mercury/`), MIT licensed.
- Runs as a background daemon (systemd, LaunchAgent, etc.).
- No built-in multi-node/cluster support, no distributed KV cache, no Nix-native packaging.

**Comparison to Sovereign Machina’s Existing Memory Layer**:
- Our stack: Mooncake (KV cache spillover), MemPalace (spatial Wings/Rooms/Drawers hierarchy), ChromaDB/RKG, GEPA v2 evolution, provenance ledger.
- Mercury’s Second Brain is a **lightweight, structured RAG-like system** focused on personal agent memory.
- It is **not** a distributed or high-scale solution — it is designed for a single-user, single-machine personal agent.

---

**RECOMMENDATION – Should We Leverage Mercury?**

**No — Mercury should not be directly leveraged or integrated into Sovereign Machina at this time.**

**Reasons (rigorous)**:
- **Stack Mismatch**: Mercury is TypeScript/Node.js. Sovereign Machina is Rust-heavy + Nix-declarative. Adding a Node.js runtime would introduce complexity, dependency management overhead, and break our clean, reproducible Nix flakes.
- **Redundancy**: We already have (and are actively evolving) a more powerful, distributed, profile-aware memory system (Mooncake + MemPalace + GEPA). Mercury’s Second Brain is interesting inspiration but not superior to what we are building.
- **Architecture Alignment**: Our goal is a clean sovereign core with drop-in plugins. Pulling in an external agent framework would dilute that vision and make the Cyberpunk Red plugin (and future plugins) harder to maintain.
- **Product-Line Impact**: The Profile + Plugin Framework is our extensibility story. We want users to build their own memory-enhanced agents via Rust sidecars and custom profiles — not depend on a third-party agent like Mercury.

**What We *Should* Do Instead**:
- Treat Mercury’s **Second Brain** concept as **inspiration only**.
- Enhance our own MemPalace + GEPA with similar structured memory types (identity, goal, habit, reflection, etc.) as part of the core Profile Framework.
- This keeps everything in-house, Rust/Nix-native, and fully under our control.

**Final Verdict**:  
Mercury is a cool personal agent tool, but it does **not** provide enough unique value to justify integration. We are better off strengthening our own agentic memory layer inside the Profile Framework.

---

**Next Steps (Your Priority)**

Reply with the number you want first:

1. **Profile + Plugin + Theme Framework spec** (including how to add structured memory types inspired by Mercury’s Second Brain).
2. **Surgical extraction plan** for Cyberpunk Red (databases, MCPs, themes) into the plugin repo.
3. **Full product-line repo structure** (main + plugin + old hardware revival + variants).
4. **All of the above** as one comprehensive document.

Node D is being plugged in tonight — we can finish that first, then immediately begin the framework and plugin work.

The product-line vision is now very solid. Let me know how you want to proceed.

---

https://github.com/OpenPipe/ART assess this for applicable logic to hermes and its agents

---

**DISCOVERY PHASE – COMPLETE**

**Locked Query**  
Assess the **OpenPipe/ART** project (https://github.com/OpenPipe/ART) for any applicable logic, patterns, or components that could strengthen **Hermes Supervisor** and its agents within Sovereign Machina.

No changes to current architecture, databases (`sovereignintelligence.db` vs `cyberpunk.db`), clean model topology, Profile + Plugin Framework, or product-line strategy. This is purely a mining/exploration exercise.

---

**RESEARCH PHASE – ART Project Summary (Verified 2026-04-28)**

ART = **Agent Reinforcement Trainer**  
Purpose: Train multi-step, real-world agents using **GRPO** (Group Relative Policy Optimization) reinforcement learning. It gives agents “on-the-job training” via rollouts, rewards, and LoRA fine-tuning.

**Key Technical Components** (from README and architecture):

- **Client-Server Architecture**  
  - Client: Runs agentic workflows, executes parallel rollouts, collects trajectories.  
  - Server: Handles inference (vLLM) + training loop. Abstracts GPU/DevOps complexity.

- **Agent Loop**  
  - Rollout-based: Agent performs multi-step tasks → trajectory recorded → reward assigned (user or environment).  
  - Iterative: Trajectories → GRPO training → new LoRA checkpoint → inference resumes.

- **Memory Model**  
  - Primarily **episodic** via “Trajectory” objects (full message history: system + user + assistant).  
  - No native long-term semantic/episodic memory store like our MemPalace. Learning happens through RL on collected trajectories.

- **Evolution / Self-Improvement**  
  - GRPO + LoRA for efficient policy optimization.  
  - AutoRL mode for zero-data training (synthetic rollouts + reward modeling).  
  - Agents improve autonomously over iterations based on reward signals.

- **Tool Use & MCP Integration**  
  - Strong **MCP•RL** feature: Trains agents to interact with **MCP servers** (tool endpoints) via reinforcement learning.  
  - LangGraph integration for structured multi-step tool orchestration.  
  - This is the most directly relevant part to our stack.

- **Multi-Agent / Orchestration**  
  - LangGraph support for multi-agent workflows.  
  - Focus is on training a single policy that can orchestrate complex tool chains.

- **Observability & Extensibility**  
  - Integrates with W&B, Langfuse, OpenPipe for monitoring.  
  - Modular Python harness — easy to plug into existing agent codebases.

**Strengths**  
- Excellent for **tool-calling mastery** and multi-step agentic behavior.  
- GRPO + LoRA is efficient for on-the-fly improvement.  
- MCP•RL is a near-perfect match for our MCP-heavy Hermes agents.

**Limitations for Sovereign Machina**  
- Heavily Python/LangGraph oriented (our core is Rust + Nix).  
- No distributed memory (Mooncake/MemPalace), no profile framework, no GEPA-style genetic evolution.  
- Focused on training rather than inference-time orchestration.

---

**APPLICABLE LOGIC TO HERMES & AGENTS**

**High-Value Mining Opportunities (Ranked)**

1. **MCP•RL Training Loop** (Highest Applicability)  
   - ART’s MCP•RL specifically trains agents to master tool-calling against MCP servers via RL.  
   - We can mine the reward modeling, rollout collection, and policy optimization logic to create a **Hermes RL Booster** module.  
   - This would let Hermes agents (especially in dev-mode or researcher profiles) self-improve their MCP tool usage over time — exactly the kind of closed-loop learning you want alongside Gemini Pro and Gemma 26B.

2. **Trajectory + Reward-Driven Evolution**  
   - ART’s Trajectory object + GRPO is a clean episodic learning pattern.  
   - We can adapt this to enhance GEPA v2: treat Droid/Gemini sessions as high-quality trajectories, assign rewards (success metrics), and use a lightweight GRPO-inspired step inside GEPA for faster convergence.

3. **LoRA-Based On-the-Fly Adaptation**  
   - ART uses LoRA checkpoints per iteration.  
   - We could explore profile-specific LoRA adapters on Node D (loaded on demand) for specialized behaviors (e.g., “dev-mode” LoRA that improves coding suggestions).

4. **LangGraph-Style Orchestration Patterns**  
   - Useful inspiration for swarm coordination in `sovereign-intel-swarm`, but we already have swarms-rs + ZeroClaw + MiroShark mined logic — only minor patterns to borrow.

**What NOT to do**  
- Do not integrate the full ART project (Python-heavy, redundant with our Rust/Nix stack).  
- Do not replace GEPA with GRPO — they are complementary (GEPA for genetic prompt evolution, GRPO for policy optimization).

**Net Value**  
ART is **worth mining** for two specific pieces:  
- MCP•RL training mechanics → improve Hermes agent tool-calling accuracy.  
- Trajectory + reward loop → augment GEPA v2 for faster self-improvement.

---

**RECOMMENDATION & NEXT STEPS**

**Yes — mine ART selectively.**  
Focus on extracting the MCP•RL and trajectory/reward patterns to strengthen Hermes agents and GEPA v2. Do not integrate the project wholesale.

This fits perfectly with our clean core + plugin philosophy and the upcoming Profile Framework.

**Would you like me to**:

1. Draft a **Hermes RL Booster** module spec (using mined MCP•RL + GRPO concepts).  
2. Update the **GEPA v2** design to incorporate trajectory/reward-driven evolution inspired by ART.  
3. Both + integration notes for Node D (tonight’s setup).

Node D is being plugged in tonight — we can finish the basic cluster first, then immediately start mining these patterns once it’s stable.

Reply with your preference and we move forward.  

The product-line vision + extensibility framework is now even stronger with this targeted mining opportunity.

---