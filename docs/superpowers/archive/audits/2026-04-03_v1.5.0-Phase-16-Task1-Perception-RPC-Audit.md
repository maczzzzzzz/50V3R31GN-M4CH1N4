# Audit: Phase 16 Task 1 — ZeroClaw Perception RPC

**Date:** 2026-04-03  
**Version:** 3.8.28-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS-SYNTHESIS  
**Phase:** 16 — Semantic Perception (Falcon Sidecar)  
**Status:** 🔧 IMPLEMENTATION COMPLETE — Pending Gemini Physical Verification

---

## Summary

Task 1 implements the `ocr_analyze` RPC on Node A (ZeroClaw Rust), including the full **Model Swap Protocol** for sequential VRAM management on the GTX 1050 Ti (4GB Pascal).

---

## Files Modified / Created

| File | Change |
|------|--------|
| `zeroclaw/Cargo.toml` | Added `base64`, `ndarray`, `ort` (CUDA); bumped version to 1.5.0 |
| `zeroclaw/src/lib.rs` | Registered `pub mod perception` |
| `zeroclaw/src/perception/mod.rs` | **New** — PerceptionController + Model Swap Protocol |
| `zeroclaw/src/server/clawlink.rs` | Added `ocr_analyze` RPC; shared `Arc<PerceptionController>` |
| `zeroclaw/src/main.rs` | Construct `Arc<PerceptionController>` on startup; pass to connections |

---

## Architecture Decisions

### Model Swap Protocol

The `PerceptionController` enforces sequential VRAM via a `tokio::sync::Mutex<VramGuard>`:

```
ocr_analyze called
  → acquire vram_lock (blocks concurrent requests)
  → POST /api/generate { keep_alive: 0 }  // unload Llama-3
  → preprocess_image (base64 → NCHW float32 tensor 1×3×224×224)
  → SessionBuilder::new(ort_env) + CUDA EP → run inference → drop Session
  → ollama_warmup(".")                     // preemptive Llama reload
  → release vram_lock → return entities
```

### ONNX Runtime (ort 1.16)

- CUDA Execution Provider selected first, CPU fallback if unavailable
- `GraphOptimizationLevel::Level3` for Pascal sm_60 throughput
- Session is scoped and dropped within `run_falcon_inference` — VRAM freed immediately
- ORT `Environment` is constructed once at startup and shared via `Arc`

### Output Contract (Stub)

`decode_outputs` returns a `PERCEPTION_STUB` sentinel entity until Gemini deploys the Falcon ONNX model file and wires the real token decoder. This allows the full RPC pipeline to be validated end-to-end before the model file exists.

---

## Unit Tests (zeroclaw/src/perception/mod.rs)

| Test | Validates |
|------|-----------|
| `test_detected_entity_serialization_round_trip` | JSON encode/decode of DetectedEntity |
| `test_detected_entity_json_fields` | Correct field names in serialized output |
| `test_preprocess_image_output_shape` | Tensor is (1, 3, 224, 224) |
| `test_preprocess_image_channel_normalisation` | R≈1.0, G≈0.0, B≈0.0 for solid red input |
| `test_preprocess_image_rejects_invalid_base64` | Error on malformed base64 |
| `test_preprocess_image_rejects_non_image_bytes` | Error on non-image bytes |
| `test_perception_config_defaults` | Config sanity — ONNX path, Ollama URL, model name |
| `test_vram_model_initial_state_is_llama` | VramGuard initialises to VramModel::Llama |
| `test_vram_model_transitions` | State enum values are distinct |
| `test_decode_empty_outputs_returns_empty_vec` | Empty ONNX output → empty Vec |

**DO NOT RUN — Hand off to Gemini for physical execution.**

---

## Handoff Checklist for Gemini

1. **`cargo build` on Node A** — confirm `ort` 1.16 + CUDA feature compiles against ONNX Runtime shared library on the GTX 1050 Ti
2. **`cargo test zeroclaw::perception`** — run the unit test suite; all 10 tests must pass
3. **ONNX model deployment** — place `models/falcon-0.3b-ocr.onnx` (or equivalent vision model) at the path in `PerceptionConfig::default()`
4. **Live RPC test** — send `{ "method": "ocr_analyze", "params": { "image": "<base64>" } }` via ClawLink and confirm `PERCEPTION_STUB` response until decoder is wired
5. **Decoder implementation** — replace `decode_outputs` stub with real token/bbox decoder matching the deployed model's output schema

*Co-Authored-By: Claude Sonnet <noreply@anthropic.com>*  
*Co-Authored-By: Gemini CLI <gemini-cli@google.com>*


---
**LINKS:** [[OS_CORE]]
