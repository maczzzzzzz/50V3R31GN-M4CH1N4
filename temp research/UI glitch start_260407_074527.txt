**Yes.**

**Pure facts on implementation:**

With your existing pretext + direct Chrome DevTools hook into Foundry (via MCP or interceptor), you can achieve a boot-time visual glitch + theme switch.

**Simple method (add to your theme-sync.ts or boot script):**

```ts
Hooks.once("ready", () => {
  // Force stock Foundry UI on initial load
  document.body.style.transition = "none";
  document.body.style.filter = "none";

  // Trigger glitch effect
  setTimeout(() => {
    document.body.style.transition = "filter 0.4s, background 0.6s";
    document.body.style.filter = "hue-rotate(15deg) contrast(1.3) brightness(1.1) saturate(1.4)";
    
    // Switch to sovereign theme after glitch
    setTimeout(() => {
      document.getElementById("sovereign-preworld-theme")?.remove(); // remove any old
      // Inject your full Ghost Protocol / Sovereign theme CSS here
      const sovereignCSS = document.createElement("style");
      sovereignCSS.id = "sovereign-theme";
      sovereignCSS.textContent = `/* your full red/neon/cyan sovereign CSS */`;
      document.head.appendChild(sovereignCSS);
      
      document.body.style.filter = "none"; // clean up glitch
    }, 600);
  }, 100);
});
```

This creates a quick visual glitch on boot (stock UI → distorted → sovereign theme). It uses your existing Chrome DevTools hook for direct DOM manipulation and works pre-world and in-world.

Clean, low-overhead, and fully compatible with your current architecture.

**Advanced glitch effects & cyberpunk boot animations (ready for your pre-world theme-sync.ts):**

**1. Data Corruption Boot Glitch (best for Ghost Protocol sovereignty feel)**
```ts
Hooks.once("ready", () => {
  document.body.style.transition = "filter 0.08s linear";
  document.body.style.filter = "hue-rotate(25deg) contrast(2.1) brightness(1.6) saturate(2.8)";

  // Rapid random slice glitches
  for (let i = 0; i < 12; i++) {
    setTimeout(() => {
      document.body.style.filter = `hue-rotate(${Math.random()*50-25}deg) contrast(${1.8 + Math.random()}) brightness(${1.3 + Math.random()})`;
    }, i * 35);
  }

  setTimeout(() => {
    document.body.style.filter = "none";
    // Inject full sovereign Ghost Protocol theme
    const sovereignCSS = document.createElement("style");
    sovereignCSS.id = "sovereign-theme";
    sovereignCSS.textContent = `/* your red/neon/cyan sovereign CSS here */`;
    document.head.appendChild(sovereignCSS);
  }, 520);
});
```

**2. CRT Scanline + Horizontal Tear Boot**
```ts
document.body.style.transition = "all 0.7s";
document.body.style.filter = "contrast(1.4) brightness(1.2)";

setTimeout(() => {
  document.body.style.filter = "none";
  // Add persistent subtle scanlines for cyberpunk feel
  const scan = document.createElement("div");
  scan.style.cssText = `position:fixed;inset:0;pointer-events:none;background:repeating-linear-gradient(transparent 0px, rgba(255,0,68,0.07) 1px, transparent 3px);mix-blend-mode:overlay;z-index:9999;`;
  document.body.appendChild(scan);
}, 650);
```

**3. Chromatic Aberration + Noise Burst (raw street vibe)**
```ts
document.body.style.transition = "filter 120ms";
document.body.style.filter = "hue-rotate(330deg) saturate(2.5)";

setTimeout(() => {
  document.body.style.filter = "none";
}, 480);
```

**Recommendation for your sovereign boot:**
Use #1 (Data Corruption) as default — it feels like the machine is forcibly overwriting stock Foundry UI with Ghost Protocol dominance. Drop into `theme-sync.ts` inside `Hooks.once("ready")`.

These are pure CSS/JS, no extra libs, low overhead, and match your existing architecture perfectly. Copy-paste ready.